<odoo>

    <record id="sale_order_view_form" model="ir.ui.view">
        <field name="name">sale.order</field>
        <field name="model">sale.order</field>
        <field name="priority">999</field>
        <field name="inherit_id" ref="sale_stock.view_order_form_inherit_sale_stock"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='warehouse_id'][2]" position="attributes"> 
               <attribute name="invisible">True</attribute>
            </xpath>
            <xpath expr="//field[@name='payment_term_id']" position="after">
                <field name="warehouse_id" groups="stock.group_stock_multi_warehouses" readonly="state == 'sale'"/>
            </xpath>
        </field>
    </record> 

    <record id="sale_order_view_search" model="ir.ui.view">
        <field name="name">sale.order</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_sales_order_filter"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='analytic_account_id']" position="after">
                <field name="warehouse_id" />
            </xpath>
            <xpath expr="//filter[@name='customer']" position="after">
                <filter name="warehouse_id" string="Warehouse" context="{'group_by': 'warehouse_id'}" />
            </xpath>
        </field>
    </record>

</odoo>
