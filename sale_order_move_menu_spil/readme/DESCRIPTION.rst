Adds invoice data (such as invoice date) to sale related moves menu. This is
achieved using ``stock_picking_invoice_link`` OCA addon, which is more accurate
retrieving invoice data from a stock move.
