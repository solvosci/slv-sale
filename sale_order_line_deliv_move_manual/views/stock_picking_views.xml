<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_picking_form" model="ir.ui.view">
        <field name="name">stock.picking.form (in sale_order_line_deliv_move_manual)</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form" />
        <field name="arch" type="xml">
            <xpath
                expr="//field[@name='move_ids_without_package']"
                position="before"
            >
                <field name="sale_id" invisible="1"/>
            </xpath>
            <xpath
                expr="//field[@name='move_ids_without_package']//tree//field[@name='quantity']"
                position="after"
            >
                <field name="product_invoice_policy" column_invisible="1"/>
                <field
                    name="qty_delivered_dest"
                    string="Dest."
                    column_invisible="not parent.sale_id"
                    readonly="product_invoice_policy != 'stock_move_dest'"
                    optional="show"
                />
            </xpath>
        </field>
    </record>

</odoo>
