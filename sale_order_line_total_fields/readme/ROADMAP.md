Summary data doesn't follow source field decimal precision definition, and always will show 2 decimal digits, recommending installing, if needed, ``web_tree_aggregate_decimal_precision addon`` addon, that will actually override this for every aggregate at every tree view across Odoo, or simply adding a custom digits attribute option for this field at the current view via an extra inherited view.
